
const radius = 30;

function setup() {
  createCanvas(640, 480);
  background(0);
}
function draw() {

  // let rndX = random(0, width)
  // let rndY = random(0, height)
  background(0);
  noStroke();
  ellipse(40, 120, radius, radius);
}
